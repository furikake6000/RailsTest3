<h5>トータルランキング</h5>

<% @ranking = User.where("is_secret = ?", false).limit(10) %>

<table class="table">
  <thead>
    <tr>
      <th scope="col">ランキング</th>
      <th scope="col">ユーザ情報</th>
    </tr>
  </thead>
  <tbody>
    <% @ranking.each_with_index do |ranker, i| %>
    <% rankeracc = @client.user(ranker.twid.to_i) %>
    <tr>
      <th scope="row"><%= i + 1 %></th>
      <td><%= render ranker %></td>
    </tr>
    <% end %>
  </tbody>
</table>
