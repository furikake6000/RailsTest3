<div class="row">
  <div class="col-md-8">
    <div class="media mb-3">
      <% icon_path = get_twpic_uri(@user_tw_account) %>
      <%= image_tag(icon_path, size: "128x128" , alt: @user_tw_account.name, class: "mr-3 rounded-circle") %>
      <div class="media-body">
        <h2 class="mt-0"><%= @user_tw_account.name %></h2>
        <p>Score: <%= @user.get_score(@client) %></p>
      </div>
    </div>

    <%= render partial: 'users/detected' %>

    <div class="card bg-light mb-3">
      <div class="card-body">
        <h3 class="card-title">Mission</h3>
        <p class="card-text">本日のあなたの暗号は
        <b>「<%= @words.join("」、「") %>」</b>
        です。</p>
        <p class="card-text">これらの単語を含んだツイートをしてください。含まれた数に応じてポイントが加算されます。</p>

        <%= render partial: 'words/words' %>
      </div>
    </div>
  </div>
  <div class="col-md-4">
    <%= render partial: 'users/friends' %>
    <%= render partial: 'users/ranking' %>
  </div>
</div>
